
# Project Goals

* build a simple GUI app to manage a list of tasks
* For now, stick to packages that are already installed on the system
* Use tkinker for the GUI elements
* Use sqlite3 for the database
* It's OK to use py_compile as a crude linter
* Do not add database migration logic to this project
* Don't create tests without being told to
